{
  "name": "repl-client",
  "version": "0.3.1",
  "description": "REPL Client (rc) with tab completion and history. A simple, npm installable alternative to netcat (nc) and socat.",
  "keywords": [
    "repl",
    "replify",
    "observability"
  ],
  "author": "Daniel D. Shaw <dshaw@dshaw.com> (http://dshaw.com)",
  "repository": {
    "type": "git",
    "url": "https://github.com/dshaw/repl-client.git"
  },
  "bugs": {
    "url": "http://github.com/dshaw/repl-client/issues"
  },
  "license": "MIT",
  "main": "./repl-client.js",
  "bin": {
    "rc": "./repl-client.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "directories": {
    "examples": "examples"
  },
  "dependencies": {
    "optimist": "^0.3.7"
  },
  "devDependencies": {},
  "engines": {
    "node": ">= 0.10.0"
  }
}
